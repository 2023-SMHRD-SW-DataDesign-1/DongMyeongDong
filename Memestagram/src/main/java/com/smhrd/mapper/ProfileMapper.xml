<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.ProfileMapper">

	<select id="showmember" resultType="ProfileDTO">
		select M.mem_img, M.mem_reward
			,(select count(F.following) from follow F where F.following = M.mem_id) AS followingnumber
			,(select count(F.follower) from follow F where F.following = M.mem_id) AS followernumber
		from member_info M
		where M.mem_id = #{mem_id};
	</select>

</mapper>
	
	